# wenxin2api runtime
# Runtime listen port inside the app/container
PORT=5001
# Docker Compose host port (compose only; container still listens on PORT)
wenxin2api_HOST_PORT=6011
LOG_LEVEL=INFO

# Admin authentication
wenxin2api_ADMIN_KEY=change-me

# Config loading (choose one)
# 1) file-based config
wenxin2api_CONFIG_PATH=/app/config.json
# 2) inline JSON or Base64 JSON
# wenxin2api_CONFIG_JSON=
# 3) legacy compatibility alias
# CONFIG_JSON=

# Optional: static admin assets path
# wenxin2api_STATIC_ADMIN_DIR=/app/static/admin
